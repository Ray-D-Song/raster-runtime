[package]
name = "raster_runtime_temporal"
description = "raster_runtime Module temporal"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
repository = "https://github.com/ray-d-song/raster_runtime"
readme = "README.md"

[lib]
name = "raster_runtime_temporal"
path = "src/lib.rs"

[dependencies]
jiff = { version = "0.2" }
raster_runtime_utils = { version = "0.1.0", path = "../../libs/raster_runtime_utils", default-features = false }
rquickjs = { version = "=0.12.1", features = ["macro"], default-features = false }
